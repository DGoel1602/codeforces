{
  "name": "codeforce-push",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "watch-rating": "bun run watch-rating.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
